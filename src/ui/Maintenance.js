import styles from "./Display.module.css";

const Maintenance = () => {
  return (
    <section className={styles.maintenance}>
      Server is under maintenance. This wont take long. Please refresh page.
    </section>
  );
};

export default Maintenance;
